---
import ProvidedIcon from "./ProvidedIcon.astro";

interface Props {
  class?: string;
  label?: string;
}

const { class: className = "h-6 w-6", label } = Astro.props;
---

<ProvidedIcon src="/images/icons/noun/location.svg" class={className} label={label} />
